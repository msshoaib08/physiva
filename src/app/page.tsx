import CallToAction from '@/components/cta.component';
import HeroPage from '@/pages/hero.page';

const page: React.FC = () => {
	return (
		<>
			<HeroPage />
			<CallToAction />
		</>
	);
};

export default page;
